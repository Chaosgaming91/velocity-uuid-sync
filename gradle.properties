# Gradle
org.gradle.jvmargs=-Xmx2G
org.gradle.parallel=true
org.gradle.configuration-cache=false

# Mod Properties
mod_version=1.0.0
maven_group=com.chaosgaming.velocityuuidforwarder
archives_base_name=velocityuuidforwarder

# Minecraft Properties
minecraft_version=1.21.1
loader_version=0.16.10
fabric_version=0.110.0+1.21.1

# Using stable Loom version instead of SNAPSHOT for production builds
# If this version doesn't work in your build environment, try:
# - 1.14-SNAPSHOT (latest but unstable)
# - 1.8.12 (older stable)
# - Check https://fabricmc.net/develop for latest stable version
loom_version=1.8-SNAPSHOT
